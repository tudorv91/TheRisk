gamestate = [1:7; randi([0 1], 1, 7); randi([1 7], 1, 7); zeros(1,7)];

neighboursMatrix = zeros(7,7);

for sourceIdx=1:7
    for destIdx = sourceIdx+1:7
        neighbours(sourceIdx, destIdx) = round(rand);
        if sourceIdx~=destIdx
           zeros(sourceIdx, destIdx) =  
        end
    end
end
neighbours
